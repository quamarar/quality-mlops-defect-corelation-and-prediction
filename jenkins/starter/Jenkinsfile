pipeline {
  agent any 
    stages {
      stage (creationofseedjob) {
        steps {
          sh """
            python3 -m venv venv
            . venv/bin/activate
             pip install python-jenkins
             pwd
             pip install -r /jenkins/starter/requirements.txt
             """
        }
      }
    }
  }

          
        
     
